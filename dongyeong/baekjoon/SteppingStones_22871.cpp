#include <iostream>
#include <queue>

using namespace std;

int calc(int a, int b) {
	if (a > b) return 1 + a - b;
	else 1 + b - a;
}

int main()
{
	int N;

	cin >> N;

	int* arr = new int[N];

	for (int i = 0; i < N; i++) {
		cin >> arr[i];
		
		e
	}

	delete[] arr;

	return 0;
}